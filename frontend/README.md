# StarForge Frontend

React + TypeScript UI for StarForge.

```bash
npm install
npm run dev -- --host 0.0.0.0 --port 3002
npm run build
```

The frontend proxies API calls to the local FastAPI backend through Vite.

